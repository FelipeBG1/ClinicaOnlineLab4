import { ArmarDatosDinamicosPipe } from './armar-datos-dinamicos.pipe';

describe('ArmarDatosDinamicosPipe', () => {
  it('create an instance', () => {
    const pipe = new ArmarDatosDinamicosPipe();
    expect(pipe).toBeTruthy();
  });
});
